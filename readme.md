# To execute the program : `python .\manage.py runserver`
# To install the requirements : `pip install -r requirements.txt`
# To install venv : `python -m venv venv`
# To create database : `python manage.py migrage`
# To view admin panel : `localhost:8000/admin` (browser)
# To view admin panel : `python manage.py createsuperuser` (terminal)
